# Example environment variables for local development / Render sync
# Copy to .env and fill in secrets (never commit real keys)

# API Keys
GROQ_API_KEY=your_groq_key_here
GOOGLE_API_KEY=your_gemini_key_here
PPLX_API_KEY=your_perplexity_key_here

# Feature Flags
DISABLE_OFFLINE_TTS=1            # 1 disables local offline fallbacks to keep container light
VOICE_DEBUG=0                    # 1 enables voice diagnostics panel
VOSK_SKIP_DL=1                   # 1 skips automatic Vosk download (set 0 if mounting model)
TTS_CLEAN_SECONDS=900            # Seconds before temp TTS files are purged
EMBED_MODEL=all-MiniLM-L6-v2     # Embedding model name; fallback hash if unavailable

# Streamlit
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_SERVER_PORT=8501

# Misc
PYTHONUNBUFFERED=1
